//Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.6);
resultado = Math.ceil(2.9); //Ceil redondea SIEMPRE HACIA ARRIBA
resultado = Math.floor(2.9); // Floor redondea SIEMPRE HACIA ABAJO
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-2); // Convierte el numero negaivo en positivo
resultado = Math.min(3, 5, 8, 1, 2, 10); // Devuelve el valor mínimo
resultado = Math.max(3, 5, 2, 3, 6, 99); // Devuelve el valor máximo
resultado = Math.random(); // Numero random
resultado = Math.floor(Math.random() * 30); // Numero random con una operación

console.log(resultado);