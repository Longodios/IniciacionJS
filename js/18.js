 //Declaracion de una funcion 
 function sumar(numero1, numero2) { // Numero1 y 2 son parámetros van dentro de la funcion
     console.log(numero1 + numero2);
 }


 // Llamada a una funcion
 sumar(10, 10);
 sumar(1, 10);
 sumar(12, 10); // SON ARGUMENTOS  o valores reales
 sumar(4, 10);

 // Expresión de la funcion
 const sumar2 = function(n1, n2) {

         console.log(n1 + n2);
     }
     //Llamada a la funcion
 sumar2(5, 10);